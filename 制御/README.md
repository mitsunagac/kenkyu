# 制御フォルダ README

SUMO（富山・昭和モデル）上で J 交差点の信号を制御するプログラム群。
`予測/` で学習した GNN モデル（`.pth` + `scaler*.pkl`）を読み込み、次サイクルの待ち台数を
予測して青時間を増減する。制御なし・ギャップ感応制御という比較用のベースラインも同じ
プログラムの中にモードとして持っている。

共通の考え方:

- **対象交差点は J**。道路1（主道路）= phase0、道路5（従道路）= phase5 の青を伸縮する。
- **delta**（青の増減秒数）を1サイクルごとに決め、`スケジュール青 + delta` を設定する。
  前サイクルの結果に足し込む（compound する）ことはしない。
- サイクル長は**時間帯別**（0-7時 100s / 7-8時 130s / 8-17時 110s / 17-19時 130s / 19-24時 100s）。
  J 以外の信号は各サイクル長にスケールしたプログラムを事前生成して切り替える。
- delta を入れ続けるとサイクル長がドリフトするので、**累積オフセット補正**（累積±20sで
  ±10s 押し戻す調整サイクル）を入れている。

## 可動範囲（青時間・サイクル長のずれの上限）

目標サイクル長が 100s でも 130s でも、**ずれの上限は絶対秒数で一定**にしている。

| 対象 | 上限 | 定数 |
| --- | --- | --- |
| 各青フェーズ（phase0 / phase5）の「その時間帯のスケジュール青」からのずれ | **±10s** | `J_GREEN_DEV_CLAMP` |
| J のサイクル長の「その時間帯の目標サイクル長」からのずれ | **±10s** | `J_CYCLE_DEV_CLAMP`（`R2_CYCLECAP` で変更可） |
| 青の絶対下限 | 5s | `J_MIN_GREEN` |

phase0 と phase5 は独立に制御されるため、放っておくと両方が同じ向きに振れてサイクル長のずれが
最大 ±20s になる。そこで delta を決めるとき `clamp_delta_for_cycle()` で
**もう一方のフェーズが既に使っている分を差し引いて**枠内に収める（切り詰めた分は累積にも乗せない）。
ギャップ感応制御も同じ枠を使い、`green_bounds_for_phase()` が返す範囲内でしか青を切らない。

各サイクル長でのスケジュール青と可動範囲:

| 目標サイクル | phase0（道路1） | phase5（道路5） |
| --- | --- | --- |
| 100s | 53s（43〜63s） | 19s（9〜29s） |
| 110s | 58s（48〜68s） | 24s（14〜34s） |
| 130s | 68s（58〜78s） | 34s（24〜44s） |

※ 括弧内は相手フェーズのずれが0のときの範囲。相手が+10s使っていれば上限側が10s狭まる。
5秒の絶対下限に引っかかるケースは全サイクル長で発生しない（最も厳しい100sのphase5で9s）。

出力は `csv掃き出し/<制御モード名>/` 配下（予測結果・フェーズ時間・遅れ時間・無駄青など）。

---

## 現行の制御プログラム

| スクリプト | 予測モデル | 赤の分割 | delta の決め方 | 出力先 (`csv掃き出し/…`) |
| --- | --- | --- | --- | --- |
| **5. 信号制御_赤2分割.py** | `予測モデル/1. 赤時間2分割/道路{1,5}_分散評価あり`<br>[前半,後半] → 次サイクルの[前半,後半] | 2分割 | 旧2分割制御の規則<br>`t3>t4 → +t3*2` / `t3<t4 → −t4*2` / 同数 → 0（±10s） | `予測制御_赤2分割`<br>`ギャップ感応制御_赤2分割`<br>`制御なし_赤2分割` |
| **4. 信号制御_赤3分割.py** | `予測モデル/3. 赤時間3分割/道路{1,5}_分散評価あり`<br>[t1,t2,t3] → [t4,t5,t6] | 3分割 | 案1(最大区間) / 案2A(前半重視) / 案2B(後半重視) / 案3。詳細は [制御案_赤3分割GNN.md](制御案_赤3分割GNN.md) | `予測制御_案2A` など案ごと |

どちらも中身（サイクル長制御・J の青更新・遅れ時間/待ち台数の全信号集計・時間距離図・
ギャップ感応制御・ログ出力）は共通で、**違うのは「予測モデル」「赤の分割数」「delta の規則」の3点だけ**。

### 制御コア（モジュール）

| ファイル | 中身 |
| --- | --- |
| `新環境用/赤2分割_制御コア.py` | 2入力2出力モデル定義 / `run_prediction_red2` / `delta_red2` / `RedSplit2Tracker`（赤2分割の実時間計測）。サイクル長と計測エッジ定義は3分割コアから import して共用 |
| `新環境用/赤3分割_制御コア.py` | 3入力3出力モデル定義 / `run_prediction_red3` / `delta_variant`（案1/2A/2B/3）/ `RedSplit3Tracker` / サイクル長スケジュール（`get_target_cycle`・`build_scaled_logic`）/ 計測エッジ定義（`MEAS_CONFIG`） |
| `新環境用/到着間隔_制御コア.py` | 待ち台数Wと最大間隔発生台数目Nの同時予測モデル。予測ギャップ制御(pgap)・N単独制御(npred)で使用 |

### 赤の分割の測り方（両方式で共通）

1. その道路の**黄が終わった瞬間**を赤開始、**青になった瞬間**を赤終了とする（フェーズ番号で判定）。
2. 赤の間、対象エッジで**初めて速度0になった車**の「赤開始からの経過秒」を記録する。
3. 赤終了時に赤の実長を2等分（3分割版は3等分）し、各区間の初停車台数を数え、`ceil(台数/2)` する。

旧2分割制御（`1. 信号制御用_従道路も.py`）は「赤開始から固定オフセット（道路1: +28s/+56s、
道路5: +45s/+90s）の瞬間に停車中の台数を数える」方式だった。この固定値は**サイクル長130s固定**を
前提にしたもので、時間帯別サイクルでは赤の別の位置を測ってしまうため、実測赤を等分する方式に変えた。
赤の間は車が列から出ていかないので「その時点で停車中の台数」≒「そこまでに初停車した台数」であり、
定義の差は前サイクルの残留車と微速前進車の扱いだけ。

---

## ギャップ感応制御の仕様（2026-08-03 確定）

**最小青から始めて、車が来る限り延長する**方式。予測制御と同じ ±10s の枠に収まる。

| 項目 | 値 |
| --- | --- |
| 開始時の青（最小青） | **BASE − 10s**（100sサイクルなら 主道路43s / 従道路9s） |
| 最大延長幅 | **20s**（最小青 → BASE + 10s） |
| 延長のしかた | 感知器に車が来るたび「今から `MAX_GAP` = 5秒先」まで割当を伸ばす（上限 MAX） |
| 終了条件 | 割当が尽きたとき（＝5秒の隙間が空いたとき）／MAX 到達時は強制的に切る |
| 感知器 | 主道路 `J_south_1` / 従道路 `J_west_0`（ループコイル） |

BASE = その時間帯のスケジュール青（`j_scheduled_green`）。予測制御の `スケジュール青 + delta` と
同じ基準なので、両方式とも「BASE ± 10s」という同一条件で比較できる。

### 累積オフセット補正（サイクル長のドリフト補正）

青が終わるたびに `偏差 = 実際の青 − BASE` を累積し、閾値に達したら調整サイクルで押し戻す。
予測制御とまったく同じ方式・同じ閾値（20s で開始、10s 未満になるまで継続、1回の補正 ±10s）。

```text
延長なしが続く場合（100sサイクル・主道路 BASE=53s）
  43s → 偏差 -10 → 累積 -10
  43s → 偏差 -10 → 累積 -20   ← 閾値到達、調整サイクルを予約
  63s → 偏差 +10 → 累積 -10   ← 調整（BASE+10s）。10s以上なので継続
  63s → 偏差 +10 → 累積   0   ← 通常に復帰
```

偏差の基準 BASE は**青の開始時点の値を控えて**使う（`main_base_at_start`）。
時間帯別サイクル長の切替が青の途中で起きると BASE が変わり、
「割り当てたときの基準」と「測るときの基準」が食い違って偏差が枠を超えるため
（修正前は +25s の例があった）。

## 時間帯別サイクル長の対象信号

基準サイクルが120sの信号だけが時間帯別スケーリングの対象。それ以外は対象外として
**無言でスキップ**される（`5. 信号制御_赤2分割.py` の `abs(base_cycle - 120) > 0.5: continue`）。

| 信号 | 基準サイクル | 時間帯別サイクル長制御 |
| --- | --- | --- |
| A, B, D, E, F, G, H, I, K, L, M, N, O, P | 120s | 対象（100 / 110 / 130s に切替） |
| J | 120s | 対象（GNN制御・ギャップ制御側で個別に適用） |
| **C** | **90s** | **対象外 — 常に90s固定** |
| J50, 1677890329 | 90s | 対象外 |
| cluster_… 5信号 | 120s | 対象 |

C だけは元から90s設計なので、他が100/110/130sに動いても90sのまま。3分割版も同じ挙動。

## 制御モード（`R2_MODE` / `R3_MODE`）

| モード | 内容 | 使う予測 |
| --- | --- | --- |
| `prediction` | GNN予測で青を伸縮（既定） | 赤2分割 or 赤3分割モデル |
| `gap` | ギャップ感応制御（車頭時間が空いたら青を切る） | 使わない |
| `none` | 制御なし（固定サイクル）。計測とログだけ | 使わない |
| `pgap` | ギャップ骨格 + 予測Nでギャップ閾値を動的に緩める | 到着間隔モデル(W・N) |
| `pgapmin` | 予測WでMIN_GREENを可変にする版 | 到着間隔モデル(W・N) |
| `npred` | ギャップを見ず「通過台数 ≧ 予測N」で青を切る | 到着間隔モデル(W・N) |

`pgap` / `pgapmin` / `npred` は赤の分割数に依存しない（道路ごとの**合計**待ち台数とNしか使わない）ため、
2分割版でもそのまま動く。

## 実行方法

```text
cd 制御/新環境用

python "5. 信号制御_赤2分割.py"                   # 赤2分割GNN予測制御（既定）
set R2_MODE=none & python "5. 信号制御_赤2分割.py"    # 制御なし（比較用ベースライン）
set R2_MODE=gap  & python "5. 信号制御_赤2分割.py"    # ギャップ感応制御

python "4. 信号制御_赤3分割.py"                   # 赤3分割GNN予測制御（案2A）
set R3_VARIANT=1 & python "4. 信号制御_赤3分割.py"   # 案1（最大区間）
```

### 環境変数

赤2分割版（`5. 信号制御_赤2分割.py`）:

| 変数 | 既定 | 意味 |
| --- | --- | --- |
| `R2_MODE` | `prediction` | 制御モード（上表） |
| `R2_VARIANT` | `red2` | `none` にすると予測はするが delta=0（予測だけ記録したいとき） |
| `R2_MODEL1` / `R2_MODEL5` | `道路1_分散評価あり` / `道路5_分散評価あり` | 使用するモデルフォルダ名 |
| `R2_EVENCANCEL` | `0` | `1` で旧2分割方式（奇数サイクル=制御 / 偶数サイクル=打ち消し） |
| `R2_DEADBAND` | `0` | 前半と後半の差がこの台数未満なら delta=0 |
| `R2_CYCLECAP` | `10` | J のサイクル長のずれの上限(秒)。青1本あたりの上限は常に±10s |
| `R2_END` | `86400` | 終了時刻(秒)。短縮実行に使う |
| `R2_GUI` | `0` | `1` で sumo-gui |
| `R2_GAPSHORT` | `2` | 予測ギャップ制御の緩めたギャップ閾値(秒) |

赤3分割版は同じ役割の変数が `R3_*`（`R3_MODE` / `R3_VARIANT` / `R3_END` / `R3_GUI` / `R3_GAPSHORT`）。
`npred` / `pgapmin` 用の細かい変数（`R3_NMIN_MAIN` など）は2分割版でも `R3_*` のまま。

---

## 動作確認の記録（赤2分割版, 2026-08-02）

1. **制御側の推論が学習側と一致することを確認済み。**
   学習データのテスト日（151日目）の全664サイクルについて、`run_prediction_red2` の出力と
   `予測モデル/1. 赤時間2分割/道路{1,5}_分散評価あり/予測結果_*.csv` を突合し、
   最大差 **2.7e-06**（float32の丸め誤差）で一致。スケーリング・ALPHA倍・逆正規化の
   経路が学習時と同じであることの裏付け。

2. **SUMO短縮実行（`R2_END=1700`）で予測8回・制御8回が発火**、エラーなし。
   道路1の予測は step 1369 → 1470 → 1570 → 1670 と約100s間隔で、
   0〜7時のサイクル長100sと一致（時間帯別サイクルが効いている）。
   出力CSV `予測結果_道路1.csv` は `回数,step,予測t3,予測t4,予測合計` の5列。

3. delta は 3分割版と同じく **float のまま `int()` で切り捨て**（`int(t3*2)`）。
   旧2分割制御は予測値を整数に丸めてから2倍していたので、最大1秒ぶん控えめになる。

4. **可動範囲の検証（2026-08-02）**
   - 机上での総当たり: サイクル長{100,110,130}s × 相手フェーズのずれ±10s × 希望delta±20s ×
     2フェーズ の全組合せで、青のずれ最大10.0s / サイクル長のずれ最大10.0s、**違反0件**。
   - SUMO実走（`R2_END=1700`）: 予測制御はログに `サイクルずれ=+1s` などを毎回出力し全て枠内。
     ギャップ感応は準備時間後（step≥1200）で 主道路 −3〜+2s / 従道路 −9〜−6s /
     サイクル長 −9〜−7s と全て±10s以内。
   - ※ シミュレーション開始直後の1本目の青だけは、traci接続時に既に進行中だった位相の
     残りを計測するため短く出る（例: G=21s）。これは準備時間(PREP_TIME=1200s)内なので
     計測・集計には入らない。

---

## 比較・レポート生成

| スクリプト | 内容 |
| --- | --- |
| `★全制御同時比較_比較レポート生成.py` | none / gap / prediction を順に実行して比較レポートを作る |
| `★赤3分割_全案比較_レポート生成.py` | 赤3分割の案1/2A/2B/3 を比較 |
| `★赤2分割_3方式比較_表4表5生成.py` | **赤2分割の3方式**（制御なし / ギャップ感応 / 予測制御）を比較し、`csv掃き出し/赤2分割/` に表4・表5・まとめCSVを出力 |
| `★表4表5_生成.py` | 遅れ時間・待ち台数の表（対象交差点J / 他交差点 / 街全体）。赤3分割ほか `csv掃き出し` 直下の方式が対象 |
| `★時間帯別_signal方式_gap比較.py`, `★N単独_4方式比較.py` | 時間帯別・N単独制御の比較 |

---

## 赤2分割 3方式の比較結果（2026-08-02, 24時間走行）

出力先 `csv掃き出し/赤2分割/`。基準は制御なし。

**表4: 街全体（全24h）**

| 方式 | 待ち台数 [台] | 差 | 遅れ時間 [s] | 差 |
| --- | --- | --- | --- | --- |
| 制御なし | 110,100 | | 2,556,831 | |
| ギャップ感応 | 108,972 | −1,128（約1%） | 2,541,856 | −14,975（約0.6%） |
| 赤2分割GNN予測制御 | 116,396 | +6,296（約6%） | 2,793,332 | +236,501（約9%） |

**表5: 対象交差点(J) / 他交差点（全24h）**

| 方式 | 待ち台数 J | 差 | 待ち台数 他 | 差 | 遅れ時間 J | 差 | 遅れ時間 他 | 差 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 制御なし | 28,232 | | 81,868 | | 675,177 | | 1,881,654 | |
| ギャップ感応 | 23,252 | −4,980（約18%） | 85,720 | +3,852（約5%） | 595,528 | −79,649（約12%） | 1,946,328 | +64,674（約3%） |
| 赤2分割GNN予測制御 | 27,634 | −598（約2%） | 88,762 | +6,894（約8%） | 726,033 | +50,856（約8%） | 2,067,299 | +185,645（約10%） |

6-19時窓も同傾向（街全体で予測制御は待ち台数 +7% / 遅れ +11%）。

わかったこと:

- **ギャップ感応が最良**。対象交差点Jで待ち台数 −18% / 遅れ −12%、街全体でもプラス。
- **赤2分割GNN予測制御は制御なしより悪化**。J では待ち台数 −2% と改善するが、
  他交差点への波及（+8%）が大きく街全体では +6%。赤3分割版でも同じ傾向
  （`5. 記録データ (7月京三)` の表4で待ち台数 +3% / 遅れ +5%）が出ているので、
  **GNN予測制御が周辺交差点に負荷を移す**という性質は分割数によらない。
- 制御ログの delta 統計: 道路1が796回・道路5が797回発火し、**平均はどちらも +0.01s**
  （範囲 ±10s）。学習データ上では正に偏っていたが、累積オフセット補正と
  サイクル長の枠制限（24hで284回作動）が効いてほぼ中立に収束した。
- **サイクル長のずれは全区間で ±10s 以内**を維持。

---

## 旧プログラム（参照用・現在は使わない）

| スクリプト | 内容 |
| --- | --- |
| `1. 信号制御用_従道路も.py` | **旧2分割制御**。`adjust_signal_A_based_on_prediction(t3, t4)` が delta の原典。奇数サイクルで制御・偶数サイクルで打ち消す方式。計測は固定オフセット |
| `2.ギャップ感応用_従道路込.py` | ギャップ感応制御の原型 |
| `3. 信号制御_mix.py` | 予測制御とギャップ感応の混合 |
| `前半後半予測_実行用.py`, `前半後半_奇偶_実行用.py`, `奇数偶数サイクル制御.py` | 前半後半予測・奇偶方式の実験版 |
| `信号制御用.py`, `信号制御用_オフセット可変.py`, `ギャップ感応用 .py` | さらに古い版 |

これら旧版は `予測モデル/0. 制御用_待ち台数単体(3.22)/道路{1,5}/` のモデル（2入力2出力・GCNConv5層）を
読み込む。**このフォルダは現行スクリプトも参照しているので消さないこと。**

---

## 既知の注意点

1. **予測モデルのパスは修正済み（2026-08-02）。**
   フォルダを 計測 / 予測 / 制御 の3構成に整理した際、予測モデルが `研究/予測モデル/` から
   `研究/予測/予測モデル/` へ移動したが、`赤3分割_制御コア.py` と `到着間隔_制御コア.py` の
   `_PRED_ROOT` が旧パスのままで **`load_road_model` が FileNotFoundError になっていた**。
   両方とも `"予測", "予測モデル", …` を経由するよう修正済み。

2. **赤2分割モデルは130s固定サイクルで学習されている。**
   学習データ（`新環境待ち台数データ_12.22.csv`, 665サイクル/日 = 130s）と、制御時の
   時間帯別サイクル（100〜130s）は条件が異なる。モデルは「赤を2等分した各区間の台数」を
   見るので比は保たれるが、厳密に学習条件へ合わせたい場合は `赤3分割_制御コア.py` の
   `CYCLE_SCHEDULE_BY_HOUR` を全時間帯130sにして、比較対象（none/gap）も同条件で回し直すこと。
   赤3分割モデルの学習データは時間帯別サイクルで計測されているのでこの問題は無い。

3. **2分割の delta は青を延長する側に偏る。**
   学習データのテスト日で試算すると delta の平均は 道路1 = +3.5s、道路5 = +3.0s
   （道路5は664サイクル中、負の delta が0回）。赤の前半に多く停まるという物理的傾向によるもの。
   旧2分割制御が偶数サイクルで打ち消していたのはこの偏りを相殺するためと思われる。
   既定の累積オフセット補正でもドリフトは抑えられるが、挙動を比べたい場合は `R2_EVENCANCEL=1`。

4. **ギャップ感応制御の青がサイクル長に追従していなかった（2026-08-02 修正）。**
   `GapCycleController` は BASE/MIN/MAX を 60/50/70s（主道路）・26/16/36s（従道路）で
   **固定**しており、さらに時間帯別サイクル長の切替時に J だけスケジュール青の更新を
   スキップしていた。そのため他の信号が100sサイクルに切り替わっても **J のギャップ制御だけ
   120s基準の青のまま**で、サイクル長が食い違っていた。
   現在は `_refresh_bounds()` が各青の開始時とサイクル長切替時に
   「その時間帯のスケジュール青 ±10s（かつサイクル ±10s）」を取り直す。
   ※ `pgapmin` / `npred` は独自のMIN/MAXを使うため `follow_schedule = False` で追従対象外。

5. **`train_csv_path` は現在どのスクリプトでも使っていない**（変数として残っているだけ）。

6. 旧スクリプトの多くは**絶対パス直書き**で、`C:\Users\Tsukasa\Desktop\研究\…` を前提にしている。
   現行の `4.` / `5.` とコアモジュールはスクリプト位置からの相対解決。
